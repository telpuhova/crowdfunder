export class Project {
  constructor (public name: string, public owner: string, public description: string, public goal: number, public reward: string, public category: string, public pledged: number) {}
}
